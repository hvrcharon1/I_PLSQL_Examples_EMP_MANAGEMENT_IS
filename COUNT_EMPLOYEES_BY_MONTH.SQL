SET SERVEROUTPUT ON
DECLARE
  V_MONTH NUMBER(2) := 1;
  V_COUNT NUMBER(3);
BEGIN

  FOR MONTH IN 1 .. 12
  LOOP
     SELECT COUNT(*) INTO V_COUNT FROM EMPLOYEES WHERE TO_CHAR(HIRE_DATE,'mm') = MONTH;
     
     DBMS_OUTPUT.PUT_LINE( TO_CHAR(month,'00') || TO_CHAR(V_COUNT,'999'));
  END LOOP;
END;
