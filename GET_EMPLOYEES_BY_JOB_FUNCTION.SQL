CREATE OR REPLACE FUNCTION GET_EMPLOYEES_BY_JOB(P_JOBID VARCHAR2)
RETURN VARCHAR2
IS
  V_NAMES VARCHAR2(1000) := '';
  CURSOR EMPCUR IS
    SELECT FIRST_NAME FROM EMPLOYEES WHERE JOB_ID = P_JOBID ORDER BY 1;
BEGIN

   FOR EMPREC IN EMPCUR
   LOOP
       V_NAMES := V_NAMES || ',' || EMPREC.FIRST_NAME;
   END LOOP;
   
  RETURN  LTRIM(V_NAMES,',');
END;

Query to use function
=========================
SELECT JOB_TITLE, GET_EMPLOYEES_BY_JOB(JOB_ID) FROM JOBS;
